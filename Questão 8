#include <stdio.h>
#include <stdlib.h>

int main(){
    
    int numero, quadrado;
    
    printf("Digite um número inteiro: ");
    scanf("%d", &numero);
    
    quadrado = numero * numero;
    
    printf("O número %d ao quadrado é: %d", numero, quadrado);

    return 0;
}
